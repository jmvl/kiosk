# Convex Backend (Production - replace with production URL when ready)
VITE_CONVEX_URL=https://clear-salmon-146.convex.site

# Kiosk Configuration (will be set per deployment)
VITE_KIOSK_ID=kiosk-001
VITE_LANGUAGE=fr

# Production
VITE_DEV_MODE=false
